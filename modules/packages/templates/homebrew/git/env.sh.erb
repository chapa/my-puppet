if [ -f <%= scope['::homebrew::config::install_dir'] %>/etc/bash_completion.d/git-completion.bash ]; then
	source <%= scope['::homebrew::config::install_dir'] %>/etc/bash_completion.d/git-completion.bash
fi
